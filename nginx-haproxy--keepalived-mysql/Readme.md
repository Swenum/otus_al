```
* Развертывание высокодоступного Web сайта

На виртуальных машинах lb1 (192.168.100.171),lb2 (192.168.100.172) установлен и настроен keepalived с общим ip 192.168.100.100, также на них настроен haproxy настроенный на балансировку бэкендов и по распределению нагрузки по алгоритму  robin-robin.
На виртуальные машины nginx1-3 установлен nginx и настроен wordpress обращающийся к mysql настроенный на хосте mysql.
Также поднимается машина с ansible для удобства менеджмента и для случая когда нет возможности установить ansible на машину администратора.
![Схема стенда](https://github.com/Swenum/otus_al/nginx-haproxy--keepalived-mysql/Schema.png "Схема стенда]")

Перед запуском провиженга необходимо поместить свой публичный ключ в файл bootstrap.sh в каталоге vagrant и сам приватный ключ в файл /ansible/ssh/

```bash
vagrant up
cd ansible
ansible-playbook -i inventory.yml site.yml -u swenum

```
